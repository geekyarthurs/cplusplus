#include <iostream>
using namespace std;

#define MAX(a, b) (a > b ? a : b)

inline function max(int a, int b)
{
    return (a > b ? a : b);
}

int main(int argc, char const *argv[])
{

    return 0;
}
